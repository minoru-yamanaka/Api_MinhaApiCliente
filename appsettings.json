{
  "Logging": { // Configurações de log da aplicação
    "LogLevel": {
      "Default": "Information", // Nível padrão de log: registra mensagens a partir de "Information" (Info, Warning, Error, Critical)
      "Microsoft.AspNetCore": "Warning" // Para os logs do framework ASP.NET Core, só registra a partir de "Warning"
    }
  },

  "AllowedHosts": "*", // Permite que a aplicação aceite requisições de qualquer host (pode ser limitado em produção)

  "ConnectionStrings": {
    "DefaultConnection": "Server=TIT0676510W11-1\\SQLEXPRESS;Database=DbClientes;Trusted_Connection=True;TrustServerCertificate=True;"
    // String de conexão do banco de dados:
    // - Server=TIT0676510W11-1\SQLEXPRESS  → servidor e instância do SQL Server
    // - Database=DbClientes                → nome do banco
    // - Trusted_Connection=True            → usa autenticação integrada do Windows
    // - TrustServerCertificate=True        → aceita certificado do servidor mesmo sem validação da autoridade
  }
}
